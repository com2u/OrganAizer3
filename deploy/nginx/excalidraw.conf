server {
  listen 80;
  listen [::]:80;
  server_name excalidraw.ai-server.org;
  include conf.d/include/letsencrypt-acme-challenge.conf;
  location / { return 301 https://$host$request_uri; }
}

server {
  listen 443 ssl;
  listen [::]:443 ssl;
  server_name excalidraw.ai-server.org;
  ssl_certificate /etc/letsencrypt/live/excalidraw.ai-server.org/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/excalidraw.ai-server.org/privkey.pem;
  include conf.d/include/ssl-ciphers.conf;

  location = /workspace-login/excalidraw {
    access_log off;
    proxy_pass http://organaizer:4815/api/workspace-auth/login/excalidraw$is_args$args;
    proxy_set_header Host $host;
  }
  location = /workspace-auth/excalidraw {
    internal;
    proxy_pass http://organaizer:4815/api/workspace-auth/check/excalidraw;
    proxy_pass_request_body off;
    proxy_set_header Content-Length "";
    proxy_set_header Cookie $http_cookie;
  }
  location / {
    auth_request /workspace-auth/excalidraw;
    proxy_pass http://excalidraw:80;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_hide_header X-Frame-Options;
    proxy_hide_header Content-Security-Policy;
    add_header Content-Security-Policy "frame-ancestors https://organaizer.app" always;
  }
}
